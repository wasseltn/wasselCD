---
- name: create image docker
  docker_image:
    name: pipe
    build:
     path: /var/lib/jenkins/workspace/pipe
     pull: no
    source: build
    state: present
    force_source: true
- name: run container docker
  docker_container:
    name: myapp_container
    image: pipe:latest
    state: started
    recreate: true
    ports:
     - "4200:80"
